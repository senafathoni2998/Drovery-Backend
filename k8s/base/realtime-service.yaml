apiVersion: v1
kind: Service
metadata:
  name: drovery-realtime
  labels: { app: drovery, role: realtime }
spec:
  type: ClusterIP
  selector: { app: drovery, role: realtime }
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
